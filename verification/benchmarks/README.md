# Engine Benchmarks

This folder stores the Rust pipeline speed gate thresholds and generated benchmark results.

Results are written under:

- `verification/results/benchmarks/<benchmark_id>/`

The active speed gate script is:

- `verification/scripts/run_rust_pipeline_speed_gate.py`
